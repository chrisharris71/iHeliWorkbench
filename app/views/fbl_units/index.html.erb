<h2>All Flybarless Controllers</h2>

<table>
  <tr>
    <th>Part number</th>
    <th>Size</th>
    <th>Manufacturer</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @fbl_units.each do |fbl_unit| %>
  <tr>
    <td><%= fbl_unit.part_number %></td>
    <td><%= fbl_unit.size %></td>
    <td><%= fbl_unit.manufacturer_id %></td>
    <td><%= link_to 'Show', fbl_unit %></td>
    <td><%= link_to 'Edit', edit_fbl_unit_path(fbl_unit) %></td>
    <td><%= link_to 'Destroy', fbl_unit, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Flybarless Controller', new_fbl_unit_path %>
